
@model Lifebuoy.Models.OffersListView

<form method="get">
    <div class="form-horizontal">
        <label class="control-label">Категория: </label>
        @Html.DropDownList("categories", Model.Categories as SelectList,
                    htmlAttributes: new { @class = "form-control minimal" })

        <label class="control-label">Провайдер: </label>
        @Html.DropDownList("providers", Model.Providers as SelectList,
                htmlAttributes: new { @class = "form-control minimal" })

        <label class="control-label">Город: </label>
        @Html.DropDownList("cities", Model.Cities as SelectList,
                htmlAttributes: new { @class = "form-control minimal" })

        <input type="submit" value="Фильтровать" class="btn btn-default" />
    </div>
</form>



@*<div class="sidebar-row">
        <div class="sidebar-row"> 
        Провайдер     
        </div>  
        <div class="sidebar-row">
            <select id="provider" class="minimal" onchange="addFilter('provider')">
                <option selected>Не выбрано</option>
                @foreach (var c in ViewBag.Providers)
                {
                    <option value="@c">@c</option>
                }
            </select>
        </div>          
</div>
<div class="sidebar-row">
        <div class="sidebar-row">
            Категория
        </div> 
        <div class="sidebar-row">
            <select id="category" class="minimal" onchange="addFilter('category')">
                <option selected>Не выбрано</option>
                @foreach (var c in ViewBag.Categories)
                {
                    <option value="@c">@c</option>
                }
            </select>
        </div> 
</div>
<div class="sidebar-row">
    <div class="sidebar-row">
        Город
    </div>
    <div class="sidebar-row">
        <select id="city" class="minimal" onchange="addFilter('city')">
            <option selected>Не выбрано</option>
            @foreach (var c in ViewBag.Cities)
            {
                <option value="@c">@c</option>
            }                      
        </select>
    </div>
</div>
<div class="row">
    Фильтры:
    <div class="sidebar-row" id="filters">
    </div>
</div>
<script type="text/javascript" src="~/Scripts/SidebarScripts.js">

</script>*@
